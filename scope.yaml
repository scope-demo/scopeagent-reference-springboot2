scope:
  metadata:
    - shell: $SHELL
  configuration:
    - shell
  mode:
    testing: true
  logger_level: info
  instrumentation:
    http:
      payloads: true
    db:
      statement_values: true