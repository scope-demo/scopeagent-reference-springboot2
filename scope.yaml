scope:
  code_path: true
  code_path_base_packages: "com.undefinedlabs.scope"
  metadata:
    shell: $SHELL
  configuration:
    - shell
  logger_level: info
  instrumentation:
    http:
      payloads: true
    db:
      statement_values: true